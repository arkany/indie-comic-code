//alert("hello world");//do I need to import jquery?//lets test for FileReader// Can we juse jquery?//var pageNumber; var pageNumber;//var testText = "Look at me, I am dialogue."//var textArray = new Array( "this is dialogue!","my my my","aren't we fancy?");var textArray = new Array();var myFont = app.textFonts.getByName("ComicGeek");//ADD LAYERSvar doc = app.activeDocument;var bubbleLayerAdd = doc.layers.add("Bubbles");bubbleLayerAdd.name = "Bubbles";var bubbleLayer = doc.layers.getByName("Bubbles");var dialogueLayerAdd = doc.layers.add("Dialogue");dialogueLayerAdd.name = "Dialogue";var dialogueLayer = doc.layers.getByName("Dialogue");//prompt what page would you like?//var pageRequest = prompt("What page would you like?", "01");//if(pageRequest !=null){//        pageNumber = pageRequest;//}// DEFINE COMIC SCRIPT FILE PATHvar docFolder = File(activeDocument.path);var folderShorten = docFolder.toString().replace("02-Lettering","");var scriptDocPath = folderShorten + ("00-assets/script.txt");//alert(scriptDocPath);//OPEN FILEvar scriptFile = File (scriptDocPath);scriptFile.open();var txtFile = scriptFile.read();scriptFile.close();theLines = txtFile.split("\n");//for each line in theLines//FIX TOMORROW for (i = 0; i < theLines.length; i++){    if (arr[i].contains(":")){            var theLine = arr[i];            //delete anything before :            textArray.push (theLine);    }}//txtFile.split("\n");//if ()//textArray.push (txtFile.split("\n"));alert(textArray[1]);//Array.push (theLines)l// open file in ('../00-assets/script.txt')// if the text 'page ' + pn exists //   alert('page exists')  // v2  // from Page X to page X + 1 // copy text//CREATE TEXT FRAMES for each item in Arrayfor ( i = 0; i < textArray.length; i++) { ///      var rectRef = doc.pathItems.rectangle(50 - (i * 100), -150, 100, 100);     var areaTextRef = doc.textFrames.areaText(rectRef);     areaTextRef.contents = testArray[i];    areaTextRef.textRange.size = 7;    areaTextRef.textRange.characterAttributes.textFont = myFont;//areaTextRef.selected = true;} // save document   //v3 // for each line // if line starts with '*:' // pull text between : and paragraph break // push to array // when done  // for each item in array // create new text box  // push text into box  //v4 // place box at 0px + 30 * (point in array) by 0px Y //save document